[project]
name = "chess-trainer"
version = "0.1.0"
description = "Chess training scheduler"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["app*"]
